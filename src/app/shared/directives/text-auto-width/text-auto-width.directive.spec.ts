import { TextAutoWidthDirective } from './text-auto-width.directive';

describe('TextAutoWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAutoWidthDirective();
    expect(directive).toBeTruthy();
  });
});
