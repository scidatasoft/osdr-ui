import { FilterBarPipePipe } from './filter-bar-pipe.pipe';

describe('FilterBarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBarPipePipe();
    expect(pipe).toBeTruthy();
  });
});
